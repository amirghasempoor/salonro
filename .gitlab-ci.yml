# GitLab CI for SalonRo
#
# Requires a runner with the Docker executor and network access to:
#   - the image registry (ArvanCloud mirror by default, see `REGISTRY`)
#   - the GitLab Container Registry ($CI_REGISTRY) for the build stage
#
# Deploy stage needs these CI/CD variables:
#   DEPLOY_HOST       - server host (IP or FQDN)
#   DEPLOY_USER       - SSH user on the server
#   DEPLOY_PATH       - absolute path to the repo clone on the server
#   DEPLOY_SSH_KEY    - SSH private key (file type, ed25519 or rsa)
#
# `$CI_REGISTRY*` variables are provided automatically by GitLab.

stages:
  - lint
  - test
#  - build
#  - deploy

variables:
  REGISTRY: docker.arvancloud.ir
  COMPOSER_CACHE_DIR: $CI_PROJECT_DIR/.composer-cache
  COMPOSER_ALLOW_SUPERUSER: "1"
  PHP_IMAGE: $REGISTRY/php:8.5.3-cli-alpine3.23
  COMPOSER_IMAGE: $REGISTRY/composer:2
  DOCKER_IMAGE: $REGISTRY/docker:27
  DIND_IMAGE: $REGISTRY/docker:27-dind

default:
  image: $COMPOSER_IMAGE
  interruptible: true
  cache:
    key:
      files:
        - backend/composer.lock
    paths:
      - backend/vendor/
      - .composer-cache/

lint:
  stage: lint
  script:
    - cd backend
    - composer install --prefer-dist --no-progress --no-interaction
    - vendor/bin/pint --test

test:
  stage: test
  script:
    - cd backend
    - composer install --prefer-dist --no-progress --no-interaction
    - cp .env.example .env
    - php artisan key:generate --ansi
    - php artisan test

#build:
#  stage: build
#  image: $DOCKER_IMAGE
#  services:
#    - $DIND_IMAGE
#  variables:
#    DOCKER_HOST: tcp://docker:2375
#    DOCKER_TLS_CERTDIR: ""
#  cache: {}
#  script:
#    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
#    - docker build -f backend/Dockerfile.production -t "$CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHORT_SHA" backend
#    - docker push "$CI_REGISTRY_IMAGE/backend:$CI_COMMIT_SHORT_SHA"
#  rules:
#    - if: '$CI_COMMIT_BRANCH == "main"'
#    - if: '$CI_COMMIT_BRANCH == "develop"'
#    - if: '$CI_COMMIT_TAG'
#
#deploy:
#  stage: deploy
#  image: $REGISTRY/alpine:3.23
#  cache: {}
#  before_script:
#    - apk add --no-cache openssh-client
#    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
#    - echo "$DEPLOY_SSH_KEY" > ~/.ssh/id_ed25519 && chmod 600 ~/.ssh/id_ed25519
#    - ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
#  script:
#    - ssh "$DEPLOY_USER@$DEPLOY_HOST"
#        "cd $DEPLOY_PATH
#        && git fetch origin main
#        && git reset --hard origin/main
#        && docker compose up -d --build"
#  environment:
#    name: production
#    url: http://$DEPLOY_HOST
#  rules:
#    - if: '$CI_COMMIT_BRANCH == "main"'
#      when: manual
